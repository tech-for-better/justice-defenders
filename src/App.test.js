// import React from 'react';
// import { render } from '@testing-library/react';
// import App from "./App"

// test.only('renders Year One Page', () => {
//   const title = "Year one"
//   const { getByText } = render(<App title/>);
//   const titleElement = getByText("Year one");
//   expect(titleElement).toBeInTheDocument();
// });

test("2 to equals 2", () => {
  expect(2).toEqual(2);
});
