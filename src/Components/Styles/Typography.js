import styled from "styled-components";

const Text = styled.p`
  font-size: 1.2rem;
`;

const Heading = styled.h2`
  font-size: 2rem;
  font-weight: bold;
  text-transform: capitalize;
`;

export { Text, Heading };
