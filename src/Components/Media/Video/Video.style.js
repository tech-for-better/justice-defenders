import styled from "styled-components";

const VideoContainer = styled.div`
  max-width: 400px;
  align-self: center;
`;

const VideoElement = styled.video`
  display: block;
  border: 1px solid black;
  border-radius: 3px;
`;

export { VideoContainer, VideoElement };
