import styled from "styled-components";
import { ReactSVG } from "react-svg";

const LogoWrapper = styled(ReactSVG)`
  height: 2rem;
  width: 2rem;
  cursor: pointer;
  margin-top: 1rem;
  position: absolute;
`;

export { LogoWrapper };
